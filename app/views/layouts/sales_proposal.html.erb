<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Proposta — AurisChat</title>
    <%= csrf_meta_tags %>
    <%= vite_client_tag %>
    <%= vite_stylesheet_tag 'sales_proposal' %>
  </head>
  <body class="bg-slate-50">
    <main class="max-w-xl mx-auto px-4 py-10">
      <%= yield %>
      <p class="mt-8 text-xs text-slate-400 text-center">
        Ao continuar, seus dados são tratados conforme a nossa política de
        privacidade. Nenhum dado de pagamento é armazenado por nós.
      </p>
    </main>
  </body>
</html>
